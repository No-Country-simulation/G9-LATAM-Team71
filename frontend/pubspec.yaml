name: wallet_flutter
description: "A modern wallet application for the Fintech Hackathon."
publish_to: 'none'

version: 1.0.0+1

environment:
  sdk: ^3.7.0

dependencies:
  flutter:
    sdk: flutter
  cupertino_icons: ^1.0.8
  nb_utils: ^7.1.4
  fl_chart: ^0.70.2
  http: ^1.2.1
  firebase_core: ^3.1.0
  firebase_messaging: ^15.0.1
  intl: ^0.19.0
  cached_network_image: ^3.4.1
  dots_indicator: ^3.0.0
  shimmer: ^3.0.0

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^5.0.0

flutter:
  uses-material-design: true
  assets:
    - assets/
